import { ProgressReportResults } from './progress-report-results';

describe('ProgressReportResults', () => {
  it('should create an instance', () => {
    expect(new ProgressReportResults()).toBeTruthy();
  });
});
