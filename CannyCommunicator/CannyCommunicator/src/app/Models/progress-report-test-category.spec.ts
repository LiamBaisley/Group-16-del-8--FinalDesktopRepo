import { ProgressReportTestCategory } from './progress-report-test-category';

describe('ProgressReportTestCategory', () => {
  it('should create an instance', () => {
    expect(new ProgressReportTestCategory()).toBeTruthy();
  });
});
